apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auto-rsa
  namespace: apps
spec:
  serviceName: auto-rsa
  replicas: 1
  selector:
    matchLabels:
      app: auto-rsa
  template:
    metadata:
      labels:
        app: auto-rsa
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: auto-rsa
          image: docker.io/nelsondane/auto-rsa:latest@sha256:eeef96a9ed322f9190f5ea737b8422b0b6c97b068cb845a1d8300b93dca451a4
          tty: true
          envFrom:
            - secretRef:
                name: rsa-secrets
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: rsa-data
              mountPath: /app/creds
  volumeClaimTemplates:
    - metadata:
        name: rsa-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
        storageClassName: longhorn
