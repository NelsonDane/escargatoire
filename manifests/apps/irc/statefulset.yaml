apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: thelounge
  namespace: apps
spec:
  serviceName: thelounge
  replicas: 1
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: thelounge
  template:
    metadata:
      labels:
        app: thelounge
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      restartPolicy: Always
      containers:
        - name: thelounge
          image: ghcr.io/thelounge/thelounge:latest@sha256:9c3667236b1a82cf79b1b35e012ddf58e1e2de46f3596befbc699825c0793680
          ports:
            - containerPort: 9000
          env:
            - name: THELOUNGE_HOME
              value: /var/opt/thelounge/data
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: thelounge-config
              mountPath: /var/opt/thelounge
          livenessProbe:
            tcpSocket:
              port: 9000
          readinessProbe:
            tcpSocket:
              port: 9000
  volumeClaimTemplates:
    - metadata:
        name: thelounge-config
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
        storageClassName: longhorn
