apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: files
  namespace: apps
  labels:
    app: files
spec:
  serviceName: files
  replicas: 1
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: files
  template:
    metadata:
      labels:
        app: files
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      restartPolicy: Always
      containers:
        - name: filestash
          image: machines/filestash:latest@sha256:745b18a165b7825e481f583f5f63c5070c35584fedc9491c004576b49dc82126
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: filestash-config
              mountPath: /app/data/state
            - name: media
              mountPath: /media
          ports:
            - containerPort: 8334
          livenessProbe:
            tcpSocket:
              port: 8334
          readinessProbe:
            tcpSocket:
              port: 8334
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: media-apps
  volumeClaimTemplates:
    - metadata:
        name: filestash-config
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
        storageClassName: longhorn
