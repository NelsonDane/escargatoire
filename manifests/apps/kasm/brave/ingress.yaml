apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brave-ingress
  namespace: apps
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
spec:
  ingressClassName: nginx
  rules:
    - host: brave.cluster.nelsondane.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: brave
                port:
                  number: 6901
