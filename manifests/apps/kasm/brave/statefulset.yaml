apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: brave
  namespace: apps
  labels:
    app: brave
spec:
  serviceName: brave
  replicas: 1
  selector:
    matchLabels:
      app: brave
  template:
    metadata:
      labels:
        app: brave
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: brave
          image: kasmweb/brave:1.17.0@sha256:e3c96c3f67fd0a13c3961bc7400d9c43180cb86392c0483a4302895d5b5a6d5c
          env:
            - name: VNCOPTIONS
              value: "-disableBasicAuth"
            - name: TZ
              value: "America/New_York"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
            - name: brave-config
              mountPath: /home/kasm-user
          ports:
            - containerPort: 6901
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
  volumeClaimTemplates:
    - metadata:
        name: brave-config
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 2Gi
        storageClassName: longhorn
