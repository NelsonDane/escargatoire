{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "kubernetes": {
    "managerFilePatterns": [
      "/^manifests/.+\\.yaml$/"
    ]
  },
  "flux": {
    "managerFilePatterns": [
      "/^manifests/.+\\.yaml$/"
    ]
  },
  "ignorePaths": [
    "manifests/flux-system/gotk-components.yaml"
  ],
  "packageRules": [
    {
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ghcr.io/siderolabs/installer"],
      "separateMajorMinor": true,
      "separateMinorPatch": true
    },
    {
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["digest"],
      "groupName": "{{namespace}} digest updates"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Group digest updates by namespace",
      "managerFilePatterns": [
        "/manifests/.+\\.yaml$/"
      ],
      "matchStrings": [
        "\\s*namespace:\\s*(?<namespace>[^\\n\\r]+)[\\s\\S]*?image:\\s*(?<depName>[^\\n\\r]+):[^\\n\\r]+@(?<currentDigest>sha256:[a-f0-9]{64})"
      ],
      "datasourceTemplate": "docker",
      "extractVersionTemplate": "^(?<version>.*)$"
    },
    {
      "customType": "regex",
      "description": "Update GitHub release references in kustomization.yaml",
      "managerFilePatterns": [
        "/manifests/.+/kustomization.ya?ml$/"
      ],
      "matchStrings": [
        "https:\\/\\/github\\.com\\/(?<depName>.*\\/.*?)\\/releases\\/download\\/(?<currentValue>.*?)\\/"
      ],
      "datasourceTemplate": "github-releases"
    },
    {
      "customType": "regex",
      "description": "Update GitHub tag references in kustomization.yaml",
      "managerFilePatterns": [
        "/manifests/.+/kustomization.ya?ml$/"
      ],
      "matchStrings": [
        "https://raw\\.githubusercontent\\.com/(?<depName>[^/]*/[^/]*)/(?<currentValue>v?\\d+\\.\\d+\\.\\d+)/"
      ],
      "datasourceTemplate": "github-tags"
    },
    {
      "customType": "regex",
      "description": "Update version references in YAML files",
      "managerFilePatterns": [
        "/^manifests/.+\\.yaml$/",
        "controlplane.yaml"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>\\S+)\\s+depName=(?<depName>\\S+)(?:\\s+versioning=(?<versioning>\\S+))?(?:\\s+extractVersion=(?<extractVersion>\\S+))?\\s*[\\r\\n]+\\s*[^:#]+:\\s*(?:[^\\s\"']+:)?(?<currentValue>v?[0-9][^\\s\"']*)"
      ]
    }
  ]
}
