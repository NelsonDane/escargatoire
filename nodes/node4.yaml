machine:
  install:
    disk: /dev/nvme0n1
  network:
    hostname: node4
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: true # Configured in OPNsense
        vip:
          ip: 10.0.2.50
cluster:
  apiServer:
    certSANs:
      - 10.0.2.54
