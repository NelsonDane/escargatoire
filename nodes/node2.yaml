machine:
  install:
    disk: /dev/nvme0n1
  network:
    hostname: node2
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: true # Configured in OPNsense
        vip:
          ip: 10.0.2.50
